{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // ← nécessaire
    "paths": {
      "@/*": ["./*"],
      "shared/*": ["../shared/*"] // ← ajout ici
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
